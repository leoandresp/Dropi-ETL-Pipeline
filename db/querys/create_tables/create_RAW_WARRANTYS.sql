CREATE OR REPLACE TABLE RAW_WARRANTYS (
    "ID GARANTIA" VARCHAR NOT NULL,
    "ID ORDEN" VARCHAR NOT NULL,
    "GUIA ORIGINAL" VARCHAR, 
    "DROPSHIPPER" VARCHAR, 
    "SUPPLIER" VARCHAR, 
    "ESTADO" VARCHAR, 
    "NUMERO DE CONTACTO TIENDA" VARCHAR, 
    "NUMERO DE PROVEEDOR" VARCHAR, 
    "PRODUCTO" VARCHAR, 
    "TIPO GARANTIA" VARCHAR, 
    "FECHA DE CREACION" VARCHAR, 
    "SHIPPING COMPANY" VARCHAR, 
    "72H SIN RESPONDER" VARCHAR, 
    "GUIA DE DESPACHO" VARCHAR, 
    "RESPUESTA PROVEEDOR" VARCHAR, 
    "RECOLECCION" VARCHAR, 
    "PROVEEDOR VERIFICADO" VARCHAR, 
    "RECOLECCION CREADA" VARCHAR, 
    "NUMERO DE GUIA O ORDEN DE RECOLECCION" VARCHAR,
    "ingestion_id" VARCHAR NOT NULL PRIMARY KEY,
    "ingestion_timestamp" DATETIME,
    "row_number" SMALLINT

);
